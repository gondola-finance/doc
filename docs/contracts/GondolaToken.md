


## Functions
### constructor
```solidity
  function constructor(
  ) public
```




### distribute
```solidity
  function distribute(
  ) public
```
Transfer `_amount` token to `_to`. Must only be called by the owner (MasterChef).



